# zerowire-sender — release Proguard rules.
# Walking-skeleton: minify is off, but keep the file so we can flip it on.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
